
services:
  web:
    build: .
    command: uvicorn uptime_monitor.main:app --host 0.0.0.0 --port 8000
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      - db
      - redis

  db:
    image: postgres:15
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "5432:5432"

  redis:
    image: redis:7
    ports:
      - "6379:6379"
  celery:
    build: .
    command: celery -A uptime_monitor.celery worker --loglevel=info
    volumes:
      - .:/code
    working_dir: /code/uptime_monitor
    depends_on:
      - db
      - redis
    env_file:
      - .env
  celery-beat:
    build: .
    command: celery -A uptime_monitor.celery beat --loglevel=info
    volumes:
      - .:/code
    working_dir: /code/uptime_monitor
    depends_on:
      - redis
      - db
    env_file:
      - .env